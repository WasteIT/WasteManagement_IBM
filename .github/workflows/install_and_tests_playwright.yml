name: playwright tests
on: 
  workflow_call: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test_playwright:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 7.0.*
    - name: Uninstall all playwright
      run: npm uninstall @playwright/test @playwright/cli @playwright/core playwright-core playwright
    - name: Install dependencies
      run: npm ci Tests/PlaywrightTests
    - name: Install NPM
      run: npm install @playwright/test@latest
    - name: Run playwright tests
      run: dotnet test Tests/PlaywrightTests --no-build --verbosity normal --configuration Debug /p:PlaywrightExecutablePath=C:\Users\runneradmin\AppData\Local\ms-playwright\chromium-1117\chrome-win\chrome.exe
